package com.reservation.controller;

import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api")
public class SeatController {
    // TODO: GET /rooms/{roomId}/seats, POST /rooms/{roomId}/seats
}
